// Stub kept for component-tree completeness; the Apply Fix flow is currently a single
// mutation + toast (no extra confirmation). Phase 7 will replace this with a richer
// preview/confirmation dialog gated by the agent's confirm_request envelope.
export function ApplyFixDialog() {
  return null;
}

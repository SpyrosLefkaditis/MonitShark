import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function CronPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Cron</CardTitle>
        <CardDescription>
          Crontab management — list, create, edit, delete, run on demand. Wired in Phase 4.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}

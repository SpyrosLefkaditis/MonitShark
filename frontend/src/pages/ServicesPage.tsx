import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function ServicesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Services</CardTitle>
        <CardDescription>
          Systemd unit inventory with start / stop / restart actions. Wired in Phase 4.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
